## Repo for testing coverage 7.0.2 performance issues with `dynamic_context`

### Install project

`poetry install`

--- or ---

`pip install -r requirements.txt`

### Run tests

`coverage run -m pytest -v`
